# OS Files
.DS_Store
Thumbs.db

# IDE / Editors
.vscode/
.idea/
*.swp
*.swo

# Environment / Logs
.env
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Node (If added later)
node_modules/
package-lock.json

# Build (If added later)
dist/
build/

# Файлове с достъпи на клиенти — НЕ се качват (репото е публично).
# Ако някой от тях наистина трябва да влезе в git: git add -f <файла>
supabase/*-setup.sql
supabase/*-accounts.sql
*credentials*
*-достъпи.*

# Локален преглед на видео поканите (генерира се от pokana.html)
_preview-*.html
